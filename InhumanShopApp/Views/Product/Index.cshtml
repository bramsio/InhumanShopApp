@using InhumanShopApp.Models.Product
@model IEnumerable<ProductInfoViewModel>
@{
    ViewData["Title"] = "All products";
}

@* различно view за admin and users *@


<div class="d-flex justify-content-between align-items-center mb-3" style="margin-top: 20px;">
    <h2>@ViewData["Title"]</h2>
    @if(User.IsInRole("Administrator"))
    {
        <a asp-controller="Product" asp-action="Add" class="btn btn-primary">
            <i class="fas fa-plus"></i> Add Product
        </a>
    }
</div>
<hr />

<div class="row text-center">
    @foreach (var product in Model)
    {
        <div class="col-md-3 mb-4">
            <div class="card h-100" style="width: 100%;">
                @if (!string.IsNullOrEmpty(product.ImageUrl))
                {
                    <img src="@product.ImageUrl" class="card-img-top" style="max-height: 150px; object-fit: contain;" alt="@product.Name">
                }
                else
                {
                    <img src="~/images/default-image.jpg" class="card-img-top" style="max-height: 150px; object-fit: contain;" alt="No Image Available">
                }

                <div class="card-body d-flex flex-column">
                    <h5 class="card-title">@product.Name</h5>
                    <p class="card-text">Category: @product.Category</p>
                    <p class="card-text"><small>Count: @product.Count</small></p>
                    <div class="d-flex justify-content-between mt-auto">
                        @if (User?.Identity?.IsAuthenticated == true)
                        {
                            <a asp-controller="Product" asp-action="Details" asp-route-id="@product.Id" class="btn btn-info me-2 w-50">
                                <i class="fas fa-info-circle"></i> Details
                            </a>
                            <form asp-controller="Product" asp-action="Add" method="post" class="w-50">
                                <input type="hidden" name="id" value="@product.Id" />
                                <button type="submit" class="btn btn-success w-100">
                                    <i class="fas fa-heart"></i> Add
                                </button>
                            </form>
                        }
                        else
                        {
                            <a asp-controller="Product" asp-action="Details" asp-route-id="@product.Id" class="btn btn-info w-100">
                                <i class="fas fa-info-circle"></i> Details
                            </a>
                        }
                    </div>
                </div>
            </div>
        </div>
    }
</div>


@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial.cshtml");
    }
}